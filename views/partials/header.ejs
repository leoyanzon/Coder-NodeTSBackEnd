<nav class="d-flex justify-content-between navbar navbar-expand-lg navbar-light bg-light px-4">
  <div>
    <a class="navbar-brand" href="/">CoderHouse Backend Cart</a>
  </div>
  <div>
    <ul class="navbar-nav mr-auto">

      <% if (typeof message.navBar != undefined && message?.navBar?.length > 0) { %>
      <% message.navBar.forEach(function(item) { %>
        <li class="nav-item px-1">
            <a class="nav-link" href=<%= item.link %>><%= item.title %></a>
        </li>
      <% }); %>
      <% }; %>

      <% if ( message?.main?.isAuthenticated ) { %>
        <li class="nav-item px-1">
          <a class="nav-link text-capitalize font-weight-bold"><%= message.main.user %></a>
      </li>
      <% }; %>
    </ul>
  </div>
    
  </nav>